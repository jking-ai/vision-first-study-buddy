import React from "react";

/**
 * MaterialList component -- displays uploaded materials as a grid of cards.
 *
 * Shows thumbnails for images and file type icons for documents.
 * Supports selecting materials for study guide or quiz generation.
 *
 * TODO: Implement with MUI components:
 * - Grid2 layout for responsive card grid
 * - Card with CardMedia (thumbnail for images) and CardContent (filename, type, size)
 * - Checkbox or selection state for multi-select
 * - Chip for file type indicator
 * - Empty state message when no materials are uploaded
 * - Loading skeleton while materials are being fetched
 */
function MaterialList({ materials, selectedIds, onSelectionChange }) {
  // TODO: Implement material list with selection
  return (
    <div>
      <p>TODO: Implement MaterialList component with cards and selection.</p>
    </div>
  );
}

export default MaterialList;

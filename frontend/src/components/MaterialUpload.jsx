import React from "react";

/**
 * MaterialUpload component -- drag-and-drop and file picker for uploading study materials.
 *
 * Supports images (JPEG, PNG, WebP), PDFs, and epub files.
 * Shows upload progress and validation errors.
 *
 * TODO: Implement with MUI components:
 * - Box with drag-and-drop zone (dashed border, drop icon)
 * - Button to open file picker as alternative
 * - Accept attribute limiting to supported file types
 * - Upload progress bar (LinearProgress from MUI)
 * - File size validation (max 20 MB)
 * - Error display for rejected files
 * - Integration with useUpload hook for API calls
 */
function MaterialUpload({ onUploadComplete }) {
  // TODO: Implement drag-and-drop and file upload UI
  return (
    <div>
      <p>TODO: Implement MaterialUpload component with drag-and-drop zone and file picker.</p>
    </div>
  );
}

export default MaterialUpload;
